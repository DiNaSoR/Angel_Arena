require('timers')

function Gobackman(trigger)
	
	trigger.activator:MoveToPosition(Vector(-3968, -1728, 129.354))
-- trigger.activator is to active the one who i am calling
-- trigger.caller is to active the caller 
end
	